public class EmailService {
    // UserDataApp userDataApp = new UserDataApp();

    String validDomains[] = { "gmail.com", "outlook.com", "hotmail.com", "zoho.com" };

    public boolean doEmailVerification(String email) {
        boolean verified = false;

        // write code here

        return verified;
    }

    public int countEmailsBasedOnDomain(String domain) {
        // write code here

        int count = 0;
        // for (User user : userDataApp.getAllUser()) {
        // if (user != null) {
        // if (user.getEmail().contains(domain)) {
        // count++;
        // }
        // }

        // }
        return count;
    }
}